class DataStorage<T> {
  public data: T[] = [];
  addItem(item: T) {
    this.data.push(item);
  }
  removeItem(item: T) {
    this.data.splice(this.data.indexOf(item), 1);
  }
  getItems() {
    return [...this.data];
  }
}

const bgc = new DataStorage<string>();
console.log(bgc.data);
